---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: directoryservices.dirsrv.operator.port389.org
spec:
  group: dirsrv.operator.port389.org
  names:
    kind: DirectoryService
    listKind: DirectoryServiceList
    plural: directoryservices
    shortNames:
    - ds
    singular: directoryservice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.replicas
      name: Replicas
      type: integer
    - jsonPath: .status.readyReplicas
      name: Ready
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          DirectoryService is the Schema for the directoryservices API.
          It represents a 389 Directory Server deployment managed by the operator.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DirectoryServiceSpec defines the desired state of a 389 Directory
              Server instance.
            properties:
              dmPasswordSecretRef:
                description: |-
                  DMPasswordSecretRef references a Secret containing the Directory Manager password.
                  The Secret must have a key named "dm-password".
                  If omitted, the operator generates a random password and creates a Secret.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              image:
                description: Image is the 389DS container image to deploy.
                minLength: 1
                type: string
              ports:
                description: Ports configures LDAP and LDAPS listener ports.
                properties:
                  ldap:
                    default: 3389
                    description: LDAP is the non-TLS listener port.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  ldaps:
                    default: 3636
                    description: LDAPS is the TLS listener port.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                type: object
              replicas:
                default: 1
                description: Replicas is the number of DS pods in the StatefulSet.
                format: int32
                minimum: 1
                type: integer
              resources:
                description: Resources defines CPU/memory requests and limits for
                  the DS container.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This is an alpha field and requires enabling the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              storage:
                description: Storage configures persistent volume claims for the /data
                  volume.
                properties:
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    default: 1Gi
                    description: Size is the PVC storage request (e.g. "10Gi").
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    description: |-
                      StorageClassName is the name of the StorageClass to use.
                      If omitted, the cluster default StorageClass is used.
                    type: string
                type: object
              suffixes:
                description: Suffixes defines the database backends to create on first
                  boot.
                items:
                  description: SuffixSpec defines a database backend (suffix) to create.
                  properties:
                    createEntries:
                      default: false
                      description: CreateEntries populates the suffix with sample
                        organizational entries on creation.
                      type: boolean
                    dn:
                      description: DN is the suffix distinguished name (e.g. "dc=example,dc=com").
                      minLength: 1
                      type: string
                    name:
                      description: Name is the backend database name (e.g. "userroot").
                      pattern: ^[a-zA-Z][a-zA-Z0-9_-]*$
                      type: string
                  required:
                  - dn
                  - name
                  type: object
                type: array
            required:
            - image
            type: object
          status:
            description: DirectoryServiceStatus defines the observed state of DirectoryService.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the instance's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              phase:
                description: Phase represents the current lifecycle phase.
                enum:
                - Initializing
                - Running
                - Degraded
                - Failed
                type: string
              readyReplicas:
                description: ReadyReplicas is the number of pods that are ready.
                format: int32
                type: integer
              replicas:
                description: Replicas is the total number of pods targeted by the
                  StatefulSet.
                format: int32
                type: integer
              suffixesReady:
                description: SuffixesReady indicates whether all configured suffixes
                  have been created.
                type: boolean
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
